extends ../1-layouts/_standard

block meta
  title= meta.works.title
  meta(name='description', content=meta.works.description)

block main
  main.main
    .parallax(data-image-src='/img/parallax-works.jpg')
      div
        h1.h1 Works

    .works
      .container
        +videoPlayer(works)
