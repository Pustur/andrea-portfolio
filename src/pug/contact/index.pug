doctype html

- const language = 'en'

html(lang=language)
  head
    title Redirecting, please wait
    meta(http-equiv='refresh', content=`0; url=${site.url}${language}${menu.contact.url}`)
  body
    p Redirecting...
